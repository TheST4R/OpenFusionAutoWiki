import Dropdown from '../components/Dropdown';
import TransportCost from '../components/TransportCost';
import EntityLink from '../components/EntityLink';
import Icon from '../components/Icon';
import InlineMeta from '../components/InlineMeta';
import AreaMap from '../components/AreaMap';
import MapSpot from '../components/MapSpot';
import { formatDurationSeconds } from '../data/formatTime';
import { transportIcon, warpIcon } from '../data/mapMarkers';

# {props.data.name}

<div className="entity-header">
  <div className="entity-meta muted">
    {props.data.zoneName && <span className="badge badge-category">{props.data.zoneName}</span>}
    {props.data.infectedZone && <span className="badge badge-warn">has infected zone</span>}
    <span className="badge badge-category">{props.data.npcs.length} NPC types</span>
    <span className="badge badge-category">{props.data.mobs.length} monster types</span>
    {props.data.missionsStarting.length > 0 && (
      <span className="badge badge-category">{props.data.missionsStarting.length} mission{props.data.missionsStarting.length === 1 ? '' : 's'} start here</span>
    )}
  </div>
</div>

{props.data.width > 0 && props.data.height > 0 && (
  <div className="area-map">
    <AreaMap area={props.data} build={props.build} />
  </div>
)}

{props.data.infectedZone && (
  <Dropdown summary="Infected Zone" open>
    <div className="infected-zone-card">
      <div className="infected-zone-header">
        <EntityLink entity={props.data.infectedZone.ref} iconSize={96} />
      </div>
      <div className="infected-zone-details">
        {props.data.infectedZone.difficultyLabel && <div><strong>Difficulty:</strong> {props.data.infectedZone.difficultyLabel}</div>}
        {props.data.infectedZone.maxScore > 0 && <div><strong>Max score:</strong> {props.data.infectedZone.maxScore.toLocaleString()}</div>}
      </div>
      {props.data.infectedZone.description && <p className="infected-zone-description">{props.data.infectedZone.description}</p>}
    </div>
  </Dropdown>
)}

{props.data.missionsStarting.length > 0 && (
  <Dropdown summary={`Missions starting here (${props.data.missionsStarting.length})`} open={props.data.missionsStarting.length <= 20}>
    <ul className="ref-list">
      {props.data.missionsStarting.map((m) => (
        <li key={m.id}><EntityLink entity={m} /></li>
      ))}
    </ul>
  </Dropdown>
)}

{props.data.npcs.length > 0 && (
  <Dropdown summary={`NPCs (${props.data.npcs.length})`}>
    <table className="location-table source-table">
      <thead>
        <tr>
          <th>NPC</th>
          <th>Count</th>
          <th>Coordinates</th>
        </tr>
      </thead>
      <tbody>
        {props.data.npcs.map((n) => (
          <tr key={n.ref.id}>
            <td><EntityLink entity={n.ref} /></td>
            <td>{n.instanceCount.toLocaleString()}</td>
            <td>
              <MapSpot
                x={n.x}
                y={n.y}
                z={n.z}
                areaId={n.areaId}
                title={n.areaZone}
                instanceName={n.instanceName}
                instanceID={n.instanceID}
                points={n.points}
                icon={n.mapIcon}
              />
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  </Dropdown>
)}

{props.data.vendors.length > 0 && (
  <Dropdown summary={`Vendors (${props.data.vendors.length})`}>
    <table className="location-table source-table">
      <thead>
        <tr>
          <th>Vendor</th>
          <th>Count</th>
          <th>Coordinates</th>
        </tr>
      </thead>
      <tbody>
        {props.data.vendors.map((v) => (
          <tr key={v.ref.id}>
            <td><EntityLink entity={v.ref} /></td>
            <td>{v.instanceCount.toLocaleString()}</td>
            <td>
              <MapSpot
                x={v.x}
                y={v.y}
                z={v.z}
                areaId={v.areaId}
                title={v.areaZone}
                instanceName={v.instanceName}
                instanceID={v.instanceID}
                points={v.points}
                icon={v.mapIcon}
              />
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  </Dropdown>
)}

{props.data.mobs.length > 0 && (
  <Dropdown summary={`Monsters (${props.data.mobs.length})`}>
    <table className="location-table source-table">
      <thead>
        <tr>
          <th>Monster</th>
          <th>Count</th>
          <th>Level</th>
          <th>HP</th>
          <th>Coordinates</th>
        </tr>
      </thead>
      <tbody>
        {props.data.mobs.map((m) => (
          <tr key={m.ref.id}>
            <td><EntityLink entity={m.ref} /></td>
            <td>{m.instanceCount.toLocaleString()}</td>
            <td>{m.level > 0 ? m.level : <span className="muted">—</span>}</td>
            <td>{m.hp > 0 ? m.hp.toLocaleString() : <span className="muted">—</span>}</td>
            <td>
              <MapSpot
                x={m.x}
                y={m.y}
                z={m.z}
                areaId={m.areaId}
                title={m.areaZone}
                instanceName={m.instanceName}
                instanceID={m.instanceID}
                points={m.points}
                icon={m.mapIcon}
              />
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  </Dropdown>
)}

{props.data.transportation.length > 0 && (
  <Dropdown summary={`Transportation (${props.data.transportation.length})`}>
    <table className="location-table source-table">
      <thead>
        <tr>
          <th>Route</th>
          <th>Operator</th>
          <th>Start</th>
          <th>Landing</th>
        </tr>
      </thead>
      <tbody>
        {props.data.transportation.map((t, i) => {
          const start = t.stops[0];
          const landing = t.stops[t.stops.length - 1];
          return (
            <tr key={`${t.routeId}-${i}`}>
              <td>
                <strong>{t.routeName}</strong>
                {t.moveType && <div><InlineMeta>{t.moveType && <span>{t.moveType}</span>}</InlineMeta></div>}
                <TransportCost cost={t.cost} />
              </td>
              <td>{t.startNpc ? <EntityLink entity={t.startNpc} /> : <span className="muted">—</span>}</td>
              <td>{start ? (
                <MapSpot x={start.x} y={start.y} z={start.z} areaId={start.areaId} title={start.areaZone} icon={transportIcon(t.moveType)} />
              ) : <span className="muted">—</span>}</td>
              <td>{landing ? (
                <MapSpot x={landing.x} y={landing.y} z={landing.z} areaId={landing.areaId} title={landing.areaZone} icon={transportIcon(t.moveType)} />
              ) : <span className="muted">—</span>}</td>
            </tr>
          );
        })}
      </tbody>
    </table>
  </Dropdown>
)}

{props.data.instanceWarps.length > 0 && (
  <Dropdown summary={`Instance warps (${props.data.instanceWarps.length})`}>
    <table className="location-table source-table">
      <thead>
        <tr>
          <th>Warp NPC</th>
          <th>Instance</th>
          <th>Entry location</th>
          <th>Required mission</th>
          <th>Level</th>
        </tr>
      </thead>
      <tbody>
        {props.data.instanceWarps.map((w, i) => (
          <tr key={`${w.id}-${w.instanceID}-${i}`}>
            <td>{w.npc ? <EntityLink entity={w.npc} /> : <span className="muted">-</span>}</td>
            <td>
              <EntityLink entity={w.instance} withIcon={false} />
              <InlineMeta leading>
                {w.requiredItem && <span>needs <EntityLink entity={w.requiredItem} withIcon={false} /></span>}
              </InlineMeta>
            </td>
            <td>{w.entryLocation ? (
              <MapSpot
                x={w.entryLocation.x}
                y={w.entryLocation.y}
                z={w.entryLocation.z}
                areaId={w.entryLocation.areaId}
                title={w.entryLocation.areaZone}
                instanceName={w.entryLocation.instanceName}
                instanceID={w.entryLocation.instanceID}
                icon={warpIcon(w.npc?.name)}
              />
            ) : <span className="muted">-</span>}</td>
            <td>
              {w.requiredMission ? <EntityLink entity={w.requiredMission} /> : <span className="muted">-</span>}
              {w.requiredTaskObjective && <div><InlineMeta leading><span>{w.requiredTaskObjective}</span></InlineMeta></div>}
            </td>
            <td>{w.requiredMinLevel > 0 ? w.requiredMinLevel : <span className="muted">-</span>}</td>
          </tr>
        ))}
      </tbody>
    </table>
  </Dropdown>
)}

{props.data.eggs.length > 0 && (
  <Dropdown summary={`Eggs (${props.data.eggs.length})`}>
    <table className="location-table source-table">
      <thead>
        <tr>
          <th>Egg</th>
          <th>Type</th>
          <th>Contains</th>
          <th>Power</th>
          <th>Duration</th>
          <th>Coordinates</th>
        </tr>
      </thead>
      <tbody>
        {props.data.eggs.map((e, i) => (
          <tr key={i}>
            <td>{e.typeName || <span className="muted">Egg</span>}</td>
            <td>{e.typeComment || <span className="muted">—</span>}</td>
            <td>{e.crateItem ? <EntityLink entity={e.crateItem} /> : <span className="muted">—</span>}</td>
            <td>
              {e.effectName ? (
                <>
                  {e.effectIcon && <Icon src={e.effectIcon} alt="" size={32} />}
                  {' '}{e.effectName}
                </>
              ) : <span className="muted">—</span>}
            </td>
            <td>{e.effectDuration > 0 ? formatDurationSeconds(e.effectDuration) : <span className="muted">—</span>}</td>
            <td>
              <MapSpot
                x={e.x}
                y={e.y}
                z={e.z}
                areaId={e.areaId}
                title={e.areaZone}
                instanceName={e.instanceName}
                instanceID={e.instanceID}
                icon="/minimap/mapicons/world_egg_shiny_npc.png"
              />
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  </Dropdown>
)}

<p className="muted alias-ids">Area key: <code>{props.data.fullName}</code></p>
