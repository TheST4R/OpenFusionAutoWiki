import { Link } from 'react-router-dom';

import Dropdown from '../components/Dropdown';
import DropChance from '../components/DropChance';
import EntityLink from '../components/EntityLink';
import Icon from '../components/Icon';
import InlineMeta from '../components/InlineMeta';
import MapSpot from '../components/MapSpot';
import SimilarItems from '../components/SimilarItems';

# {props.data.name}

<div className="entity-header">
  {props.data.icon ? <div className="entity-header-icon"><Icon src={props.data.icon} alt={props.data.name} size={192} className="icon-item" /></div> : null}
  <div className="entity-meta muted">
    <span className="badge badge-category">{props.data.type}</span>
    {props.data.displayType && props.data.displayType !== props.data.type && <span className="badge badge-category">{props.data.displayType}</span>}
    {props.data.rarity && <span className="badge badge-category">{props.data.rarity}</span>}
    {props.data.contentLevel > 0 && <span className="badge badge-category">Level {props.data.contentLevel}</span>}
    {!props.data.obtainable && <span className="badge badge-warn">unobtainable</span>}
  </div>
</div>

{props.data.description && (
  <blockquote className="npc-comment">{props.data.description}</blockquote>
)}

<dl className="stat-grid">
  {props.data.playerPrice > 0 && <><dt>Player price</dt><dd>{props.data.playerPrice.toLocaleString()} Taros</dd></>}
  {props.data.buyPrice > 0 && <><dt>Buy</dt><dd>{props.data.buyPrice.toLocaleString()} Taros</dd></>}
  {props.data.sellPrice > 0 && <><dt>Sell</dt><dd>{props.data.sellPrice.toLocaleString()} Taros</dd></>}
  {props.data.singleDamage > 0 && <><dt>Damage (single)</dt><dd>{props.data.singleDamage.toLocaleString()}</dd></>}
  {props.data.multiDamage > 0 && <><dt>Damage (multi)</dt><dd>{props.data.multiDamage.toLocaleString()} × {props.data.numberOfTargets} targets</dd></>}
  {props.data.weaponType && props.data.weaponType !== 'None' && <><dt>Weapon class</dt><dd>{props.data.weaponType}</dd></>}
  {props.data.range && props.data.range !== 'None' && <><dt>Range</dt><dd>{props.data.range}{props.data.rangeValue ? ' (' + props.data.rangeValue + ')' : ''}</dd></>}
  {props.data.rateOfFire > 0 && <><dt>Rate of fire</dt><dd>{props.data.rateOfFire}</dd></>}
  {props.data.defense > 0 && <><dt>Defense</dt><dd>{props.data.defense.toLocaleString()}</dd></>}
  {props.data.typeId === 10 && props.data.vehicleClass > 0 && <><dt>Speed class</dt><dd>{props.data.vehicleClass.toLocaleString()}</dd></>}
  {props.data.maxStack > 1 && <><dt>Stack</dt><dd>{props.data.maxStack}</dd></>}
  {props.data.gender && props.data.gender !== 'Any' && <><dt>Gender</dt><dd>{props.data.gender}</dd></>}
  <><dt>Tradeable</dt><dd>{props.data.tradeable ? 'Yes' : 'No'}</dd></>
  <><dt>Sellable</dt><dd>{props.data.sellable ? 'Yes' : 'No'}</dd></>
</dl>

{(() => {
  const areaIdFor = (areaZone) => {
    if (!areaZone || areaZone === 'Unknown - Unknown') return '';
    const slug = areaZone
      .toLowerCase()
      .replace(/\s*-\s*/g, '-')
      .replace(/[^a-z0-9-]+/g, '-')
      .replace(/-{2,}/g, '-')
      .replace(/^-+|-+$/g, '');
    return slug || areaZone.toLowerCase();
  };
  const locationLine = (areaZone) => {
    if (!areaZone) return null;
    const areaId = areaIdFor(areaZone);
    return (
      <div className="muted item-source-location">
        <em>{areaId ? <Link to={'/' + props.build + '/areas/' + areaId}>{areaZone}</Link> : areaZone}</em>
      </div>
    );
  };
  const droppedBy = props.data.sources
    .filter((s) => s.kind === 'mob')
    .slice()
    .sort((a, b) => {
      const ma = Math.max(a.boyProbability, a.girlProbability);
      const mb = Math.max(b.boyProbability, b.girlProbability);
      return (mb - ma) || a.mob.name.localeCompare(b.mob.name);
    });
  const characterCreationSources = props.data.sources
    .filter((s) => s.kind === 'character-creation')
    .slice()
    .sort((a, b) => a.genderId - b.genderId);
  const characterCreationGenders = [...new Map(
    characterCreationSources.map((s) => [s.genderId, s.gender]),
  ).values()];
  const characterCreationGenderNames = new Set(characterCreationGenders.map((gender) => gender.toLowerCase()));
  const characterCreationAudience = characterCreationGenderNames.has('any')
    || (characterCreationGenderNames.has('male') && characterCreationGenderNames.has('female'))
    ? 'Any characters'
    : characterCreationGenders.length === 1
      ? characterCreationGenders[0] + ' characters'
      : characterCreationGenders.slice(0, -1).join(', ') + ' and '
        + characterCreationGenders[characterCreationGenders.length - 1] + ' characters';
  const buckets = [
    ['Mission rewards', props.data.sources.filter((s) => s.kind === 'mission' || s.kind === 'mission-crate')],
    ['Sold by', props.data.sources.filter((s) => s.kind === 'vendor')],
    ['Eggs', props.data.sources.filter((s) => s.kind === 'egg')],
    ['Racing rewards', props.data.sources.filter((s) => s.kind === 'racing')],
    ['Code redemptions', props.data.sources.filter((s) => s.kind === 'code')],
    ['Event drops', props.data.sources
      .filter((s) => s.kind === 'event')
      .slice()
      .sort((a, b) => {
        const ma = Math.max(a.boyProbability, a.girlProbability);
        const mb = Math.max(b.boyProbability, b.girlProbability);
        return (mb - ma) || a.eventName.localeCompare(b.eventName);
      })],
  ];
  return (
    <>
      {characterCreationSources.length > 0 && (
        <Dropdown summary="Character creation" open>
          <ul className="ref-list">
            <li><strong>Available to:</strong> {characterCreationAudience}</li>
          </ul>
        </Dropdown>
      )}

      {droppedBy.length > 0 && (
        <Dropdown summary={'Dropped by (' + droppedBy.length + ')'} open={droppedBy.length <= 10}>
          <ul className="ref-list">
            {droppedBy.map((s, i) => (
              <li key={i}>
                <EntityLink entity={s.mob} />
                {locationLine(s.areaZone)}
                <InlineMeta className="muted source-card-meta">
                  <DropChance data={s} leadingSeparator={false} />
                </InlineMeta>
              </li>
            ))}
          </ul>
        </Dropdown>
      )}

      {props.data.containingCrates.length > 0 && (
        <Dropdown summary={'Containing CRATEs (' + props.data.containingCrates.length + ')'} open={props.data.containingCrates.length <= 10}>
          <ul className="ref-list">
            {props.data.containingCrates.map((c, i) => (
              <li key={c.ref.id + '-' + i}>
                <EntityLink entity={c.ref} />
                <InlineMeta className="muted source-card-meta">
                  <DropChance data={c} leadingSeparator={false} />
                </InlineMeta>
              </li>
            ))}
          </ul>
        </Dropdown>
      )}

      {props.data.typeId === 9 && props.data.crateDrops.length > 0 && (
        <Dropdown summary={'Drops (' + props.data.crateDrops.length + ')'} open>
          <ul className="ref-list">
            {props.data.crateDrops.map((d, i) => (
              <li key={d.ref.id + '-' + i}>
                <EntityLink entity={d.ref} />
                <InlineMeta className="muted source-card-meta">
                  <DropChance data={d} leadingSeparator={false} />
                </InlineMeta>
              </li>
            ))}
          </ul>
        </Dropdown>
      )}

      {buckets
        .filter(([, list]) => list.length > 0)
        .map(([label, list]) => (
          <Dropdown key={label} summary={label + ' (' + list.length + ')'} open={list.length <= 10}>
            {list[0]?.kind === 'vendor' ? (
              <table className="location-table source-table">
                <thead>
                  <tr>
                    <th>Seller</th>
                    <th>Price</th>
                    <th>Location</th>
                  </tr>
                </thead>
                <tbody>
                  {list.map((s, i) => (
                    <tr key={i}>
                      <td><EntityLink entity={s.npc} /></td>
                      <td>{s.price > 0 ? s.price.toLocaleString() + ' Taros' : <span className="muted">-</span>}</td>
                      <td>{s.areaZone || <span className="muted">-</span>}</td>
                    </tr>
                  ))}
                </tbody>
              </table>
            ) : list[0]?.kind === 'egg' ? (
              <table className="location-table source-table">
                <thead>
                  <tr>
                    <th>Egg</th>
                    <th>Location</th>
                    <th>Drop chance</th>
                  </tr>
                </thead>
                <tbody>
                  {list.map((s, i) => (
                    <tr key={i}>
                      <td>
                        <strong>{s.eggName || 'Egg'}</strong>
                      </td>
                      <td>
                        {s.areaZone ? (
                          <MapSpot
                            x={s.x}
                            y={s.y}
                            z={s.z}
                            areaId={s.areaId}
                            title={s.areaZone}
                            instanceName={s.instanceName}
                            icon="/minimap/mapicons/world_egg_shiny_npc.png"
                          />
                        ) : <span className="muted">-</span>}
                      </td>
                      <td><DropChance data={s} leadingSeparator={false} /></td>
                    </tr>
                  ))}
                </tbody>
              </table>
            ) : (
              <ul className="ref-list">
                {list.map((s, i) => {
                  if (s.kind === 'mission' || s.kind === 'mission-crate') {
                    return (
                      <li key={i}>
                        <span className="task-label requirement-label">{s.kind === 'mission-crate' ? 'From Reward CRATE:' : 'Direct Reward:'}</span>
                        <EntityLink entity={s.mission} />
                        <InlineMeta leading>{s.selectionNeeded && <span>choose one</span>}</InlineMeta>
                      </li>
                    );
                  }
                  if (s.kind === 'racing') {
                    return (
                      <li key={i}>
                        {s.infectedZone ? (
                          <EntityLink entity={{
                            ...s.infectedZone,
                            icon: s.infectedZone.icon || '/ui/ep/ep_big_' + String(s.infectedZone.id).padStart(2, '0') + '.png',
                          }} />
                        ) : <strong>{s.instanceName || 'Race'}</strong>}
                        <InlineMeta leading>
                          {s.requiredScore > 0 && <span>Score {s.requiredScore.toLocaleString()}+</span>}
                          {s.requiredStars > 0 && <span>{s.requiredStars}★</span>}
                        </InlineMeta>
                      </li>
                    );
                  }
                  if (s.kind === 'code') {
                    return <li key={i}><EntityLink entity={s.ref} withIcon={false} /></li>;
                  }
                  if (s.kind === 'event') {
                    return (
                      <li key={i}>
                        <strong>{s.eventName || 'Event #' + s.eventId}</strong>
                        <DropChance data={s} />
                      </li>
                    );
                  }
                  return null;
                })}
              </ul>
            )}
          </Dropdown>
        ))}
    </>
  );
})()}

{props.data.sources.length === 0 && props.data.crateDrops.length === 0 && props.data.containingCrates.length === 0 && (
  <p className="muted">No known sources for this item.</p>
)}

<SimilarItems current={props.data} />

<p className="muted alias-ids">
  <InlineMeta>
    <span>Type ID <code>{props.data.typeId}</code></span>
    <span>Item ID <code>{props.data.itemId}</code></span>
  </InlineMeta>
</p>
